Fix the Git configuration error in my project.

Error: "Make sure you configure your user.name and user.email in git."

Configure Git properly for this project by setting:
- git user.name
- git user.email

Check the existing Git configuration first. If missing, set both values and verify.
